apply plugin: 'jacoco'
apply plugin: 'java'
apply plugin: 'de.hhu.stups.sablecc'

dependencies {
    sableCC(rootProject.sableCCDependency)
}

rootProject.setupTestReport(project, ["de.be4.eventbalg.core.parser"])
