apply plugin: 'jacoco'
apply plugin: 'java-library'
apply plugin: 'eclipse'

dependencies {
    api project(":prologlib")
    api project(":bparser")
    api project(":ltlparser")
}

rootProject.setupTestReport(project, [])
