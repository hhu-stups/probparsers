apply plugin: 'java-library'
apply plugin: 'groovy'
apply plugin: 'jacoco'

dependencies {
    api project(":prologlib")
    api project(":bparser")
    testImplementation group: "org.codehaus.groovy", name: "groovy", version: "2.5.6"
}

rootProject.setupTestReport(project, [])
