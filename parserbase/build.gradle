plugins {
	id("java-library")
	id("jacoco")
}

description = "Library for uniform access to the formal language parsers (used by ltlparser)"

dependencies {
	api project(":prologlib")
}

rootProject.setupTestReport(project, [])
